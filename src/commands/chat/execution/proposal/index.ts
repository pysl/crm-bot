import { ChatInputCommandInteraction } from 'discord.js';
import { ns } from '../../builders/proposal';

export async function proposal(interaction: ChatInputCommandInteraction<'cached'>) {
	const { locale } = interaction;
}
